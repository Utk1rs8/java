public class TernaryOperator {
    public static void main(String[] args)
    {
        String hasArgs = args.length == 0 ? "No Arguments Required" : args[0];
        System.out.println(hasArgs);
    }
    
}
